<h1 class="show-page-title"><%= @page.title %></h1>

<div class="pages-container">
  <div class="pages-img">
    <img src="<%= @page.photo %>" class="show-page-img" alt="">
  </div>
  <div class="pages-content">
    <p class="content-text"><% @page.content %></p>
  </div>
</div>


<a href="<%= @page.photo.url %>"> Pobierz</a>

Wyświetlenia: <%= @page.views %>